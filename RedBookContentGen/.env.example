# RedBookContentGen 环境变量配置示例
# 复制此文件为 .env 并填入实际值

# ============================================
# 基础配置
# ============================================

# 主机端口（默认：8080）
HOST_PORT=8080

# ============================================
# API 配置
# ============================================

# OpenAI API Key（必填）
# 获取方式：https://dashscope.console.aliyun.com/apiKey
OPENAI_API_KEY=sk-your-api-key-here

# OpenAI 模型（可选，默认：qwen-max）
# 可选值：qwen-turbo（快速）、qwen-plus（平衡）、qwen-max（最强）
OPENAI_MODEL=qwen-max

# OpenAI Base URL（可选，默认：阿里云通义千问）
OPENAI_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# ============================================
# 图片生成配置
# ============================================

# 图片生成模式（可选，默认：template）
# 可选值：template（离线模板，推荐）、api（AI生成，需要API Key）
IMAGE_GENERATION_MODE=template

# 模板风格（可选，默认：retro_chinese）
# 可选值：retro_chinese、modern_minimal、vintage_film、warm_memory、ink_wash
TEMPLATE_STYLE=retro_chinese

# ============================================
# 日志配置
# ============================================

# 日志级别（可选，默认：INFO）
# 可选值：DEBUG、INFO、WARNING、ERROR、CRITICAL
LOG_LEVEL=INFO

# 日志格式（可选，默认：json）
# 可选值：json、text
LOG_FORMAT=json

# ============================================
# 缓存配置
# ============================================

# 是否启用缓存（可选，默认：true）
CACHE_ENABLED=true

# 缓存过期时间（秒，可选，默认：3600）
CACHE_TTL=3600

# ============================================
# 速率限制配置
# ============================================

# OpenAI API 每分钟最大请求数（可选，默认：60）
RATE_LIMIT_OPENAI_RPM=60

# 图片生成 API 每分钟最大请求数（可选，默认：10）
RATE_LIMIT_IMAGE_RPM=10

# 图片生成最大并发数（可选，默认：3）
RATE_LIMIT_IMAGE_CONCURRENT=3

# ============================================
# 日志收集配置（可选）
# ============================================

# Elasticsearch 配置
# ELASTICSEARCH_ENABLED=false
# ELASTICSEARCH_HOSTS=http://localhost:9200
# ELASTICSEARCH_USERNAME=
# ELASTICSEARCH_PASSWORD=

# Logstash 配置
# LOGSTASH_ENABLED=false
# LOGSTASH_HOST=localhost
# LOGSTASH_PORT=5000

# ============================================
# 日志告警配置（可选）
# ============================================

# HTTP 告警通知器
# HTTP_AUTHORIZATION_HEADER=Bearer your-token-here

# 邮件告警通知器
# SMTP_USERNAME=your-email@example.com
# SMTP_PASSWORD=your-password
