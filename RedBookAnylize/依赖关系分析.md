# 项目依赖关系分析

## 📊 模块依赖关系图

```
┌─────────────────────────────────────────────────────────────┐
│                        主程序入口                            │
│                    main.py / main_standalone.py              │
└─────────────────────────────────────────────────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
        ▼                   ▼                   ▼
┌──────────────┐   ┌──────────────┐   ┌──────────────┐
│   crawler    │   │   analyzer   │   │   content    │
│   (爬虫)     │   │   (分析器)   │   │  generator   │
└──────────────┘   └──────────────┘   └──────────────┘
        │                   │                   │
        │                   │                   │
        ▼                   ▼                   ▼
┌──────────────┐   ┌──────────────┐   ┌──────────────┐
│  raw_data    │   │  processed   │   │  analysis    │
│  .csv        │   │  _data.csv   │   │  _report.json│
└──────────────┘   └──────────────┘   └──────────────┘
                            │                   │
                            │                   │
                            ▼                   ▼
                    ┌──────────────┐   ┌──────────────┐
                    │  generated   │   │   visual     │
                    │  _content    │   │  _prompts    │
                    │  .json       │   │  .json       │
                    └──────────────┘   └──────────────┘
                            │                   │
                            └─────────┬─────────┘
                                      │
                                      ▼
                            ┌──────────────┐
                            │  visual      │
                            │  generator   │
                            │  (视觉生成)  │
                            └──────────────┘
```

---

## 🔗 代码依赖关系

### 1. main.py（主程序）

**依赖的模块：**
- ✅ `config` - 配置文件（必需）
- ✅ `analyzer` - 数据分析器（必需）
- ✅ `content_generator` - 内容生成器（必需）
- ✅ `visual_generator` - 视觉提示词生成器（必需）
- ⚠️ `crawler` - 爬虫模块（可选，条件导入）

**依赖的文件：**
- 输入：无（直接启动）
- 输出：通过其他模块生成文件

**执行顺序：**
```
main.py
  ├─> crawler.py (步骤1: 数据采集)
  │     └─> 生成: raw_articles.csv
  │
  ├─> analyzer.py (步骤2: 数据分析)
  │     ├─> 读取: raw_articles.csv
  │     ├─> 生成: processed_articles.csv
  │     └─> 生成: analysis_report.json
  │
  ├─> content_generator.py (步骤3: 内容生成)
  │     ├─> 读取: analysis_report.json
  │     └─> 生成: generated_content.json
  │
  └─> visual_generator.py (步骤4: 视觉提示词生成)
        ├─> 读取: generated_content.json
        └─> 生成: visual_prompts.json
```

---

### 2. crawler.py（爬虫模块）

**依赖的模块：**
- ✅ `config` - 配置文件（必需）
- ✅ `pandas` - 数据处理（必需）
- ✅ `DrissionPage` - 网页爬虫（必需）

**依赖的文件：**
- 输入：无（从网络爬取）
- 输出：`raw_articles.csv`

**被依赖：**
- `analyzer.py` - 需要读取 `raw_articles.csv`

**依赖关系：**
```
crawler.py
  └─> 生成 raw_articles.csv
       └─> 被 analyzer.py 使用
```

---

### 3. analyzer.py（分析器）

**依赖的模块：**
- ✅ `config` - 配置文件（必需）
- ✅ `pandas` - 数据处理（必需）
- ✅ `numpy` - 数值计算（必需）
- ✅ `jieba` - 中文分词（必需）
- ⚠️ `matplotlib` - 数据可视化（可选）
- ⚠️ `seaborn` - 统计图表（可选）

**依赖的文件：**
- 输入：`raw_articles.csv`（由 crawler.py 生成）
- 输出：
  - `processed_articles.csv`（处理后的数据）
  - `analysis_report.json`（分析报告）

**被依赖：**
- `content_generator.py` - 需要读取 `analysis_report.json`

**依赖关系：**
```
analyzer.py
  ├─> 读取 raw_articles.csv (来自 crawler.py)
  ├─> 生成 processed_articles.csv
  └─> 生成 analysis_report.json
       └─> 被 content_generator.py 使用
```

---

### 4. content_generator.py（内容生成器）

**依赖的模块：**
- ✅ `config` - 配置文件（必需）
- ✅ `openai` - AI API调用（必需）

**依赖的文件：**
- 输入：`analysis_report.json`（由 analyzer.py 生成）
- 输出：`generated_content.json`（生成的内容）

**被依赖：**
- `visual_generator.py` - 需要读取 `generated_content.json`

**依赖关系：**
```
content_generator.py
  ├─> 读取 analysis_report.json (来自 analyzer.py)
  └─> 生成 generated_content.json
       └─> 被 visual_generator.py 使用
```

---

### 5. visual_generator.py（视觉提示词生成器）

**依赖的模块：**
- ✅ `config` - 配置文件（必需）
- ✅ `openai` - AI API调用（必需）

**依赖的文件：**
- 输入：`generated_content.json`（由 content_generator.py 生成）
- 输出：`visual_prompts.json`（视觉提示词）

**被依赖：**
- 无（最终输出）

**依赖关系：**
```
visual_generator.py
  ├─> 读取 generated_content.json (来自 content_generator.py)
  └─> 生成 visual_prompts.json (最终输出)
```

---

### 6. config.py（配置文件）

**依赖的模块：**
- ✅ `os` - 系统操作（必需）
- ✅ `logging` - 日志记录（必需）

**依赖的文件：**
- 无（纯配置）

**被依赖：**
- 所有模块都依赖 `config.py`

**依赖关系：**
```
config.py
  └─> 被所有模块使用
       ├─> main.py
       ├─> crawler.py
       ├─> analyzer.py
       ├─> content_generator.py
       └─> visual_generator.py
```

---

## 📁 文件依赖关系

### 数据流

```
1. crawler.py
   └─> 生成: data/raw_articles.csv
        │
        ▼
2. analyzer.py
   ├─> 读取: data/raw_articles.csv
   ├─> 生成: data/processed_articles.csv
   └─> 生成: output/analysis_report.json
        │
        ▼
3. content_generator.py
   ├─> 读取: output/analysis_report.json
   └─> 生成: output/generated_content.json
        │
        ▼
4. visual_generator.py
   ├─> 读取: output/generated_content.json
   └─> 生成: output/visual_prompts.json
```

---

## 🔄 执行流程依赖

### 完整流程（main.py）

```
步骤1: run_crawler()
  ├─> 依赖: crawler.py
  ├─> 依赖: config.py
  ├─> 依赖: DrissionPage, pandas
  └─> 生成: raw_articles.csv
       │
       ▼ (必需，否则终止)
步骤2: run_analyzer()
  ├─> 依赖: analyzer.py
  ├─> 依赖: config.py
  ├─> 依赖: pandas, numpy, jieba
  ├─> 读取: raw_articles.csv (必需)
  ├─> 生成: processed_articles.csv
  └─> 生成: analysis_report.json
       │
       ▼ (必需，否则终止)
步骤3: run_content_generator()
  ├─> 依赖: content_generator.py
  ├─> 依赖: config.py
  ├─> 依赖: openai
  ├─> 读取: analysis_report.json (必需)
  └─> 生成: generated_content.json
       │
       ▼ (必需，否则终止)
步骤4: run_visual_generator()
  ├─> 依赖: visual_generator.py
  ├─> 依赖: config.py
  ├─> 依赖: openai
  ├─> 读取: generated_content.json (必需)
  └─> 生成: visual_prompts.json
```

---

## ⚠️ 关键依赖关系

### 1. 必需的文件依赖

| 步骤 | 输入文件 | 生成者 | 使用者 | 是否必需 |
|------|---------|--------|--------|---------|
| 步骤2 | `raw_articles.csv` | crawler.py | analyzer.py | ✅ 必需 |
| 步骤3 | `analysis_report.json` | analyzer.py | content_generator.py | ✅ 必需 |
| 步骤4 | `generated_content.json` | content_generator.py | visual_generator.py | ✅ 必需 |

### 2. 模块依赖链

```
config.py (所有模块都依赖)
    │
    ├─> crawler.py
    │     └─> 生成 raw_articles.csv
    │
    ├─> analyzer.py
    │     ├─> 读取 raw_articles.csv
    │     └─> 生成 analysis_report.json
    │
    ├─> content_generator.py
    │     ├─> 读取 analysis_report.json
    │     └─> 生成 generated_content.json
    │
    └─> visual_generator.py
          ├─> 读取 generated_content.json
          └─> 生成 visual_prompts.json
```

---

## 🎯 独立运行的可能性

### 可以独立运行的模块

1. **crawler.py** ✅
   - 可以单独运行
   - 只需要：config.py, pandas, DrissionPage
   - 生成：raw_articles.csv

2. **analyzer.py** ✅
   - 可以单独运行
   - 需要：raw_articles.csv（必需）
   - 只需要：config.py, pandas, numpy, jieba

3. **content_generator.py** ✅
   - 可以单独运行
   - 需要：analysis_report.json（必需）
   - 只需要：config.py, openai

4. **visual_generator.py** ✅
   - 可以单独运行
   - 需要：generated_content.json（必需）
   - 只需要：config.py, openai

### 不能独立运行的模块

- **main.py** ❌
  - 依赖所有其他模块
  - 是工作流管理器

---

## 📋 依赖总结

### 代码依赖

```
main.py
  ├─> config.py (必需)
  ├─> crawler.py (可选，条件导入)
  ├─> analyzer.py (必需)
  ├─> content_generator.py (必需)
  └─> visual_generator.py (必需)

crawler.py
  └─> config.py (必需)

analyzer.py
  └─> config.py (必需)

content_generator.py
  └─> config.py (必需)

visual_generator.py
  └─> config.py (必需)
```

### 文件依赖

```
raw_articles.csv
  └─> 由 crawler.py 生成
       └─> 被 analyzer.py 使用

analysis_report.json
  └─> 由 analyzer.py 生成
       └─> 被 content_generator.py 使用

generated_content.json
  └─> 由 content_generator.py 生成
       └─> 被 visual_generator.py 使用

visual_prompts.json
  └─> 由 visual_generator.py 生成
       └─> 最终输出
```

---

## 💡 使用建议

### 1. 完整流程（推荐）

```bash
python3 main.py -k "关键词"
```

**依赖链：**
```
crawler → analyzer → content_generator → visual_generator
```

### 2. 跳过爬虫

```bash
python3 main.py --skip-crawler -k "关键词"
```

**前提条件：**
- 必须有 `raw_articles.csv` 文件

**依赖链：**
```
analyzer → content_generator → visual_generator
```

### 3. 只运行爬虫

```bash
python3 main.py --crawler-only -k "关键词"
```

**依赖：**
- 只需要 crawler.py 和 config.py

### 4. 只运行分析

```bash
python3 main.py --analyzer-only
```

**前提条件：**
- 必须有 `raw_articles.csv` 文件

**依赖：**
- analyzer.py 和 config.py

### 5. 只生成内容

```bash
python3 main.py --content-only
```

**前提条件：**
- 必须有 `analysis_report.json` 文件

**依赖：**
- content_generator.py 和 config.py

### 6. 只生成视觉提示词

```bash
python3 main.py --visual-only
```

**前提条件：**
- 必须有 `generated_content.json` 文件

**依赖：**
- visual_generator.py 和 config.py

---

## 🔍 依赖检查清单

### 运行完整流程前检查

- [ ] `config.py` 存在
- [ ] 所有依赖包已安装
- [ ] API Key 已配置（用于 content_generator 和 visual_generator）

### 运行各步骤前检查

**步骤1（爬虫）：**
- [ ] `config.py` 存在
- [ ] `DrissionPage` 已安装
- [ ] `pandas` 已安装

**步骤2（分析）：**
- [ ] `raw_articles.csv` 存在
- [ ] `pandas`, `numpy`, `jieba` 已安装

**步骤3（内容生成）：**
- [ ] `analysis_report.json` 存在
- [ ] `openai` 已安装
- [ ] API Key 已配置

**步骤4（视觉提示词）：**
- [ ] `generated_content.json` 存在
- [ ] `openai` 已安装
- [ ] API Key 已配置

---

## 📊 依赖关系矩阵

| 模块 | config | crawler | analyzer | content_gen | visual_gen | pandas | numpy | jieba | openai | DrissionPage |
|------|--------|---------|----------|-------------|------------|--------|-------|-------|--------|--------------|
| main.py | ✅ | ⚠️ | ✅ | ✅ | ✅ | - | - | - | - | - |
| crawler.py | ✅ | - | - | - | - | ✅ | - | - | - | ✅ |
| analyzer.py | ✅ | - | - | - | - | ✅ | ✅ | ✅ | - | - |
| content_gen.py | ✅ | - | - | - | - | - | - | - | ✅ | - |
| visual_gen.py | ✅ | - | - | - | - | - | - | - | ✅ | - |

**图例：**
- ✅ 必需依赖
- ⚠️ 可选依赖（条件导入）
- - 无依赖

---

## 🎯 总结

### 核心依赖关系

1. **config.py** - 所有模块都依赖
2. **文件依赖链** - 严格顺序：raw → processed → report → content → prompts
3. **模块依赖链** - 严格顺序：crawler → analyzer → content_gen → visual_gen

### 关键点

- ✅ 每个步骤都依赖前一步的输出文件
- ✅ 如果前一步失败，后续步骤无法执行
- ✅ 可以使用 `--skip-*` 参数跳过某些步骤
- ✅ 跳过步骤时，必须确保输入文件存在

### 建议

- **完整流程：** 按顺序执行所有步骤
- **部分执行：** 确保输入文件存在
- **错误处理：** 检查依赖文件是否存在

---

**最后更新：** 2026-01-17  
**状态：** ✅ 完整分析
