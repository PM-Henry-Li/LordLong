# 依赖关系快速参考

## 🎯 一句话总结

**所有模块都依赖 `config.py`，执行流程有严格的顺序：爬虫 → 分析 → 内容生成 → 视觉提示词生成**

---

## 📊 执行流程依赖

```
main.py (主程序)
  │
  ├─> 步骤1: crawler.py
  │     └─> 生成: raw_articles.csv ✅
  │
  ├─> 步骤2: analyzer.py
  │     ├─> 需要: raw_articles.csv ✅ (必需)
  │     └─> 生成: analysis_report.json ✅
  │
  ├─> 步骤3: content_generator.py
  │     ├─> 需要: analysis_report.json ✅ (必需)
  │     └─> 生成: generated_content.json ✅
  │
  └─> 步骤4: visual_generator.py
        ├─> 需要: generated_content.json ✅ (必需)
        └─> 生成: visual_prompts.json ✅
```

---

## ⚠️ 关键依赖

### 文件依赖（严格顺序）

| 步骤 | 需要文件 | 生成文件 | 如果缺少会怎样 |
|------|---------|---------|---------------|
| 步骤1 | 无 | `raw_articles.csv` | - |
| 步骤2 | `raw_articles.csv` | `analysis_report.json` | ❌ **程序终止** |
| 步骤3 | `analysis_report.json` | `generated_content.json` | ❌ **程序终止** |
| 步骤4 | `generated_content.json` | `visual_prompts.json` | ❌ **程序终止** |

### 代码依赖

| 模块 | 依赖的模块 | 依赖的包 |
|------|-----------|---------|
| `main.py` | config, analyzer, content_gen, visual_gen | - |
| `crawler.py` | config | pandas, DrissionPage |
| `analyzer.py` | config | pandas, numpy, jieba |
| `content_generator.py` | config | openai |
| `visual_generator.py` | config | openai |

---

## 🔄 可以跳过的步骤

### 使用 `--skip-crawler`

```bash
python3 main.py --skip-crawler -k "关键词"
```

**前提：** 必须有 `raw_articles.csv` 文件

**执行流程：**
```
跳过步骤1 → 步骤2 → 步骤3 → 步骤4
```

### 单独运行某个步骤

```bash
# 只运行爬虫
python3 main.py --crawler-only

# 只运行分析（需要 raw_articles.csv）
python3 main.py --analyzer-only

# 只生成内容（需要 analysis_report.json）
python3 main.py --content-only

# 只生成视觉提示词（需要 generated_content.json）
python3 main.py --visual-only
```

---

## ✅ 检查清单

### 运行完整流程前

- [ ] `config.py` 存在
- [ ] 所有依赖包已安装
- [ ] API Key 已配置

### 运行各步骤前

**步骤1（爬虫）：**
- [ ] `config.py` 存在
- [ ] `DrissionPage`, `pandas` 已安装

**步骤2（分析）：**
- [ ] ✅ `raw_articles.csv` 存在（必需）

**步骤3（内容生成）：**
- [ ] ✅ `analysis_report.json` 存在（必需）
- [ ] `openai` 已安装
- [ ] API Key 已配置

**步骤4（视觉提示词）：**
- [ ] ✅ `generated_content.json` 存在（必需）
- [ ] `openai` 已安装
- [ ] API Key 已配置

---

## 💡 关键点

1. **config.py 是核心** - 所有模块都依赖它
2. **文件依赖是严格的** - 前一步的输出是下一步的输入
3. **如果前一步失败，后续步骤无法执行**
4. **可以使用 `--skip-*` 参数跳过某些步骤**
5. **跳过步骤时，必须确保输入文件存在**

---

## 🚨 常见错误

### 错误1：缺少输入文件

```
❌ 数据文件不存在
原因：前一步未执行或失败
解决：先执行前一步，或使用 --skip-crawler
```

### 错误2：模块导入失败

```
❌ ModuleNotFoundError
原因：依赖包未安装
解决：pip3 install 缺失的包
```

### 错误3：API调用失败

```
❌ API调用失败
原因：API Key未配置或无效
解决：检查 config.py 中的 API Key
```

---

## 📖 详细文档

查看 `依赖关系分析.md` 获取完整的依赖关系分析。

---

**快速参考版本：** 2026-01-17
